import { NewappdirDirective } from './newappdir.directive';

describe('NewappdirDirective', () => {
  it('should create an instance', () => {
    const directive = new NewappdirDirective();
    expect(directive).toBeTruthy();
  });
});
